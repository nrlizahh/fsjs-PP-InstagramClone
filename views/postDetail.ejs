<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Detail Post</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen flex justify-center items-center">
    <div class="bg-white p-8 rounded-lg shadow-md w-full max-w-md">
        <h2 class="text-3xl font-semibold text-center text-gray-800">Detail Post</h2>

        <div class="space-y-6 mt-8"" >
          
            <div class="flex flex-col space-y-2">
                <div class="border-2 border-dashed border-gray-300 p-4 rounded-lg">
                    <img class="w-full h-64 bg-gray-200 rounded-lg flex items-center justify-center text-gray-500" src="<%= post.imageURL %>" alt="Post Image">
                </div>
            </div>

            <div>
                <label for="caption" class="block text-sm font-medium text-gray-700">Caption</label>
                <p><%= post.caption %></p>
            </div>
        
            <div>
                <label for="tags" class="block text-sm font-medium text-gray-700">Select Tags</label>
                <p><%= tags %></p>
            </div>

            <div>
                <label for="tags" class="block text-sm font-medium text-gray-700">Post Date</label>
                <p><%= timeAgo(post.createdDate) %></p>
            </div>
          
            <div class="mt-6 flex justify-center">
                <a href="/profile/<%= post.UserId %>" class="inline-block px-6 py-2 text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 rounded-md transition mr-4">
                    Back
                </a>
               
                <a href="/profile/<%= post.UserId %>/post/<%= post.id %>/delete" class="inline-block px-6 py-2 text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50 rounded-md transition">
                    Delete
                </a>
            </div>
            
        </div>
    </div>

</body>
</html>
